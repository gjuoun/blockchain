- Command available (Do not run any files unless the instructions say to): 
    - Run python files with `python <file_name>`
    - Install dependencies with `uv add <package_name>`
- Use types everywhere possible